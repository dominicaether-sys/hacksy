<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hacksy - Exam Predictor Pro</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Courier New', monospace;
        }
        
        body {
            background-color: #0a0a12;
            color: #00ff41;
            line-height: 1.6;
            overflow-x: hidden;
            background-image: 
                radial-gradient(circle at 10% 20%, rgba(0, 255, 65, 0.05) 0%, transparent 20%),
                radial-gradient(circle at 90% 60%, rgba(0, 255, 65, 0.05) 0%, transparent 20%),
                linear-gradient(to bottom, #0a0a12, #050510);
            min-height: 100vh;
            padding: 20px;
        }
        
        .container {
            max-width: 1000px;
            margin: 0 auto;
            border: 1px solid #00ff41;
            border-radius: 5px;
            box-shadow: 0 0 20px rgba(0, 255, 65, 0.3);
            background-color: rgba(10, 10, 18, 0.95);
            overflow: hidden;
            position: relative;
        }
        
        .container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 2px;
            background: linear-gradient(to right, transparent, #00ff41, transparent);
            animation: scanline 3s linear infinite;
        }
        
        @keyframes scanline {
            0% { transform: translateY(-100%); }
            100% { transform: translateY(1000%); }
        }
        
        @keyframes glitch {
            0% { text-shadow: 2px 2px 0px #ff00ff, -2px -2px 0px #00ffff; }
            100% { text-shadow: -2px -2px 0px #ff00ff, 2px 2px 0px #00ffff; }
        }
        
        @keyframes blink {
            0%, 100% { opacity: 1; }
            50% { opacity: 0; }
        }
        
        @keyframes pulse {
            0% { opacity: 0.6; }
            50% { opacity: 1; }
            100% { opacity: 0.6; }
        }
        
        header {
            padding: 30px;
            text-align: center;
            border-bottom: 1px solid #00ff41;
            background: linear-gradient(to right, rgba(0, 255, 65, 0.1), rgba(0, 255, 65, 0.05));
            position: relative;
            overflow: hidden;
        }
        
        header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.1'/%3E%3C/svg%3E");
            opacity: 0.3;
        }
        
        h1 {
            font-size: 3.5rem;
            margin-bottom: 10px;
            text-transform: uppercase;
            letter-spacing: 5px;
            text-shadow: 0 0 10px #00ff41;
            animation: glitch 5s infinite alternate;
            position: relative;
        }
        
        .tagline {
            font-size: 1.2rem;
            color: #00ff9d;
            margin-top: 15px;
            text-shadow: 0 0 5px #00ff9d;
        }
        
        .main-content {
            padding: 30px;
        }
        
        .upload-section {
            background: rgba(0, 20, 15, 0.5);
            border: 1px solid #00ff41;
            border-radius: 5px;
            padding: 25px;
            margin-bottom: 30px;
            text-align: center;
            position: relative;
            overflow: hidden;
            box-shadow: 0 0 15px rgba(0, 255, 65, 0.2);
        }
        
        .upload-section::before {
            content: '>';
            position: absolute;
            top: 25px;
            left: 15px;
            color: #00ff41;
            font-weight: bold;
            animation: blink 1s infinite;
        }
        
        .upload-icon {
            font-size: 3rem;
            color: #00ff41;
            margin-bottom: 20px;
            text-shadow: 0 0 10px #00ff41;
        }
        
        .file-input {
            display: none;
        }
        
        .file-label {
            background: linear-gradient(to right, #003b1f, #006b3a);
            color: #000;
            padding: 12px 25px;
            border-radius: 3px;
            cursor: pointer;
            display: inline-block;
            margin: 15px 0;
            transition: all 0.3s;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
            border: 1px solid #00ff41;
            box-shadow: 0 0 10px rgba(0, 255, 65, 0.5);
        }
        
        .file-label:hover {
            background: linear-gradient(to right, #004d29, #008045);
            transform: translateY(-2px);
            box-shadow: 0 0 15px rgba(0, 255, 65, 0.7);
        }
        
        .file-info {
            background: rgba(0, 30, 20, 0.7);
            padding: 15px;
            border-radius: 3px;
            margin-top: 20px;
            display: none;
            border: 1px dashed #00ff41;
            text-align: left;
        }
        
        .file-info::before {
            content: '$>';
            margin-right: 10px;
            color: #00ff41;
            font-weight: bold;
        }
        
        .mode-selection {
            display: flex;
            justify-content: space-between;
            margin: 40px 0;
            flex-wrap: wrap;
            gap: 20px;
        }
        
        .mode {
            flex: 1;
            min-width: 250px;
            text-align: center;
            padding: 30px 20px;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.3s;
            background: rgba(5, 15, 15, 0.8);
            border: 1px solid #00ff41;
            box-shadow: 0 0 10px rgba(0, 255, 65, 0.3);
            position: relative;
            overflow: hidden;
        }
        
        .mode.locked {
            cursor: not-allowed;
            opacity: 0.8;
        }
        
        .mode::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: linear-gradient(to right, transparent, #00ff41, transparent);
            opacity: 0;
            transition: opacity 0.3s;
        }
        
        .mode:hover::before {
            opacity: 1;
        }
        
        .mode:hover:not(.locked) {
            transform: translateY(-5px);
            box-shadow: 0 5px 20px rgba(0, 255, 65, 0.5);
        }
        
        .mode.selected {
            background: rgba(0, 30, 15, 0.8);
            border: 2px solid #00ff41;
            box-shadow: 0 0 20px rgba(0, 255, 65, 0.7);
        }
        
        .mode-1 { border-color: #ff2a6d; box-shadow: 0 0 10px rgba(255, 42, 109, 0.3); }
        .mode-2 { border-color: #05d9e8; box-shadow: 0 0 10px rgba(5, 217, 232, 0.3); }
        .mode-3 { border-color: #00ff41; box-shadow: 0 0 10px rgba(0, 255, 65, 0.3); }
        
        .mode-1.selected { box-shadow: 0 0 20px rgba(255, 42, 109, 0.7); }
        .mode-2.selected { box-shadow: 0 0 20px rgba(5, 217, 232, 0.7); }
        .mode-3 { box-shadow: 0 0 20px rgba(0, 255, 65, 0.5); }
        
        .mode-icon {
            font-size: 2.5rem;
            margin-bottom: 20px;
        }
        
        .mode-1 .mode-icon { color: #ff2a6d; text-shadow: 0 0 10px rgba(255, 42, 109, 0.7); }
        .mode-2 .mode-icon { color: #05d9e8; text-shadow: 0 0 10px rgba(5, 217, 232, 0.7); }
        .mode-3 .mode-icon { color: #00ff41; text-shadow: 0 0 10px rgba(0, 255, 65, 0.7); }
        
        .mode-title {
            font-size: 1.5rem;
            margin-bottom: 15px;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 2px;
        }
        
        .mode-1 .mode-title { color: #ff2a6d; }
        .mode-2 .mode-title { color: #05d9e8; }
        .mode-3 .mode-title { color: #00ff41; }
        
        .mode-desc {
            color: #cccccc;
            font-size: 0.95rem;
            line-height: 1.5;
            margin-bottom: 15px;
        }
        
        .coming-soon {
            display: inline-block;
            background: linear-gradient(to right, #ff2a6d, #ff5e8e);
            color: #000;
            padding: 5px 15px;
            border-radius: 3px;
            font-weight: bold;
            font-size: 0.9rem;
            margin-top: 10px;
            animation: pulse 2s infinite;
        }
        
        .predict-btn {
            display: block;
            width: 100%;
            padding: 18px;
            background: linear-gradient(to right, #003b1f, #006b3a);
            color: #000;
            border: 2px solid #00ff41;
            border-radius: 3px;
            font-size: 1.3rem;
            cursor: pointer;
            transition: all 0.3s;
            margin: 30px 0;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 2px;
            box-shadow: 0 0 15px rgba(0, 255, 65, 0.5);
            text-shadow: 0 0 2px rgba(0, 0, 0, 0.5);
        }
        
        .predict-btn:hover {
            background: linear-gradient(to right, #004d29, #008045);
            transform: translateY(-3px);
            box-shadow: 0 0 20px rgba(0, 255, 65, 0.7);
        }
        
        .predict-btn:disabled {
            background: #1a1a2e;
            color: #666;
            border-color: #333;
            cursor: not-allowed;
            transform: none;
            box-shadow: none;
        }
        
        .progress-container {
            height: 10px;
            background: #0d1f18;
            border-radius: 5px;
            margin: 20px 0;
            overflow: hidden;
            display: none;
            border: 1px solid #00ff41;
            box-shadow: 0 0 10px rgba(0, 255, 65, 0.3);
        }
        
        .progress-bar {
            height: 100%;
            background: linear-gradient(to right, #00ff41, #00ff9d);
            border-radius: 5px;
            width: 0%;
            transition: width 0.5s ease;
        }
        
        .results-section {
            display: none;
            margin-top: 40px;
            animation: fadeIn 0.7s ease;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .results-header {
            text-align: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 1px solid #00ff41;
        }
        
        .results-title {
            font-size: 2rem;
            color: #00ff41;
            margin-bottom: 10px;
            text-transform: uppercase;
            letter-spacing: 3px;
            text-shadow: 0 极 10px #00ff41;
        }
        
        .accuracy-badge {
            display: inline-block;
            background: rgba(0, 255, 65, 0.2);
            color: #00ff41;
            padding: 8px 20px;
            border-radius: 3px;
            font-weight: bold;
            margin-top: 15px;
            border: 1px solid #00ff41;
            text-shadow: 0 0 5px #00ff41;
        }
        
        .prediction-cards {
            display: flex;
            flex-direction: column;
            gap: 25px;
        }
        
        .card {
            border-radius: 5px;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            transition: transform 0.3s;
            border: 极 solid;
            background: rgba(5, 15, 15, 0.8);
        }
        
        .card:hover {
            transform: translateY(-5px);
        }
        
        .card-header {
            padding: 18px 25px;
            color: #000;
            font-weight: bold;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 1.2rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .card-body {
            padding: 25px;
        }
        
        .topic-list {
            list-style-type: none;
        }
        
        .topic-item {
            padding: 15px 20px;
            border-bottom: 1px solid rgba(0, 255, 65, 0.2);
            display: flex;
            align-items: center;
            transition: background 0.2s;
        }
        
        .topic-item:hover {
            background: rgba(0, 255, 65, 0.05);
        }
        
        .topic-item:last-child {
            border-bottom: none;
        }
        
        .topic-item::before {
            content: '>';
            margin-right: 15px;
            font-weight: bold;
            color: #00ff41;
        }
        
        .high { border-color: #ff2a6d; }
        .high .card-header { background: #ff2a6d; }
        
        .medium { border-color: #05d9e8; }
        .medium .card-header { background: #05d9e8; }
        
        .low { border-color: #00ff41; }
        .low .card-header { background: #00ff41; }
        
        .guarantee-badge {
            background: rgba(0, 0, 0, 0.3);
            padding: 5px 15px;
            border-radius: 3px;
            font-size: 0.9rem;
        }
        
        .terminal-text {
            color: #00ff41;
            font-family: 'Courier New', monospace;
            margin: 15px 0;
            padding: 10px;
            background: rgba(0, 0, 0, 极.3);
            border-left: 3px solid #00ff41;
        }
        
        .upgrade-section {
            background: rgba(255, 42, 109, 0.1);
            border: 1px solid #ff2a6d;
            border-radius: 5px;
            padding: 25px;
            margin: 30px 0;
            text-align: center;
        }
        
        .upgrade-title {
            font-size: 1.8rem;
            color: #ff2a6极;
            margin-bottom: 15px;
            text-transform: uppercase;
            letter-spacing: 2px;
            text-shadow: 0 0 10px rgba(255, 42, 109, 0.7);
        }
        
        .upgrade-btn {
            display: inline-block;
            background: linear-gradient(to right, #ff2a6d, #极f5e8e);
            color: #000;
            padding: 15px 30px;
            border-radius: 3px;
            cursor: pointer;
            transition: all 0.3s;
            font-weight: bold;
           极ext-transform: uppercase;
            letter-spacing: 1px;
            border: 1px solid #ff2a6d;
            box-shadow: 0 0 15px rgba(255, 42, 109, 0.5);
            margin-top: 15px;
            text-decoration: none;
        }
        
        .upgrade-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 0 20px rgba(255, 42, 109, 0.7);
        }
        
        footer {
            text-align: center;
            padding: 25px;
            color: #00ff9d;
            font-size: 0.9rem;
            margin-top: 30px;
            border-top: 1px solid #00ff41;
            text-shadow: 0 0 5px #00ff9d;
        }
        
        @media (max-width: 768px) {
            .mode-selection {
                flex-direction: column;
            }
            
            .mode {
                min-width: auto;
            }
            
            h1 {
                font-size: 2.5rem;
            }
        }
        
        .glowing-text {
            text-shadow: 0 0 10px currentColor;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>HACKSY</h1>
            <p class="tagline">CRACK THE EXAM CODE - PREDICT. STUDY. DOMINATE.</p>
        </header>
        
        <div class="main-content">
            <div class="upload-section">
                <i class="upload-icon fas fa-file-code"></i>
                <h3>UPLOAD YOUR SYLLABUS</h3>
                <p>SUPPORTED FORMATS: PDF, DOCX, TXT, IMAGES</p>
                <input type="file" id="file-input" class="file-input" accept=".pdf,.docx,.txt,.jpg,.png">
                <label for="file-input" class="file-label">
                    <i class="fas fa-terminal"></i> SELECT FILE
                </label>
                <div class="file-info" id="file-info">
                    <span id="file-name">NO FILE SELECTED</span>
                    <div class极progress-container">
                        <div class="progress-bar" id="progress-bar"></div>
                    </div>
                </div>
            </div>
            
            <div class="terminal-text">
                <div>> SYSTEM: AWAITING SYLLABUS UPLOAD</div>
                <div>> ANALYZER: READY</div>
                <div>> PREDICTION ENGINE: STANDBY</div>
            </div>
            
            <h3>SELECT PREDICTION MODE:</h3>
            <div class="mode-selection">
                <div class="mode mode-1" data-mode="just-pass">
                    <i class="mode-icon fas fa-bolt"></i>
                    <div class="mode-title">JUST PASS</div>
                    <p class="mode-desc">MINIMAL EFFORT FOR 40+ MARKS. FOCUS ONLY ON CRITICAL TOPICS.</p>
                </div>
                
                <div class="mode mode-2" data-mode="decent">
                    <极 class="mode-icon fas fa-chart-line"></i>
                    <div class="mode-title">BE DECENT</div>
                    <p class="mode-desc">SOLID UNDERSTANDING FOR 60-70 MARKS. BALANCED COVERAGE.</p>
                </div>
                
                <div class="mode mode-3 locked" data-mode="dominate">
                    <i class="mode-icon fas fa-trophy"></i>
                    <div class="mode-title">DOMINATE</div>
                    <p class="mode-desc">COMPLETE MASTERY FOR 85+ MARKS. FULL TOPIC COVERAGE.</p>
                    <div class="coming-soon">COMING SOON</div>
                </div>
            </div>
            
            <div class="upgrade-section">
                <h3 class="upgrade-title">UNLOCK THE FULL POTENTIAL</h3>
                <p>DOMINATE mode is coming soon with advanced AI algorithms for 90%+ accuracy</p>
                <a href="#" class="upgrade-btn"><i class="fas fa-unlock"></i> GET NOTIFIED AT LAUNCH</a>
            </div>
            
            <button class="predict-btn" id="predict-btn" disabled>
                <i class="fas fa-brain"></i> INITIATE PREDICTION ANALYSIS
            </button>
            
            <div class="results-section" id="results-section">
                <div class="results-header">
                    <h2 class="results-title">PREDICTION ANALYSIS COMPLETE</h2>
                    <p>BASED ON SYLLABUS ANALYSIS AND SELECTED MODE</p>
                    <div class="accuracy-badge">
                        <i class="fas fa-shield-alt"></i> 89.7% PREDICTION ACCURACY
                    </div>
                </div>
                
                <div class="terminal-text">
                    <div>> ANALYSIS: SYLLABUS DECRYPTED - 42 TOPICS IDENTIFIED</div>
                    <div>> PREDICTION: EXAM CONTENT MAPPED WITH 89.7% CONFIDENCE</div>
                    <div>> RECOMMENDATION: FOCUS ON HIGHLIGHTED TOPICS FOR OPTIMAL RESULTS</div>
                </div>
                
                <div class="prediction-cards">
                    <div class="card high">
                        <div class="card-header">
                            <span><i class="fas fa-exclamation-circle"></i> HIGH PROBABILITY (90%+)</span>
                            <span class="guarantee-badge">CRITICAL</span>
                        </div>
                        <div class="card-body">
                            <ul class="topic-list" id="high-topics">
                                <li class="topic-item">FINANCIAL STATEMENT ANALYSIS</li>
                                <li class="topic-item">DOUBLE-ENTRY BOOKKEEPING</li>
                                <li class="topic-item">TAX CALCULATION METHODS</li>
                                <li class="topic-item">DEPRECIATION METHODS</li>
                            </ul>
                        </div>
                    </div>
                    
                    <div class="card medium">
                        <div class="card-header">
                            <span><i class="fas fa-info-circle"></i> MEDIUM PROBABILITY (60-89%)</span>
                            <span class="guarantee-badge">IMPORTANT</span>
                        </div>
                        <div class="card-body">
                            <ul class="topic-list" id="medium-topics">
                                <li class="topic-item">WORKING CAPITAL MANAGEMENT</li>
                                <li class="topic-item">STANDARD COSTING</li>
                                <li class="topic-item">CASH FLOW MANAGEMENT</li>
                                <li class="topic-item">INVENTORY VALUATION METHODS</li>
                            </ul>
                        </div>
                    </div>
                    
                    <div class="card low">
                        <div class="card-header">
                            <span><i class="fas fa-low-vision"></i> LOW PROBABILITY (BELOW 60%)</span>
                            <span class="guarantee-badge">OPTIONAL</span>
                        </div>
                        <div class="card-body">
                            <ul class="topic-list"极="low-topics">
                                <li class="topic-item">INTERNATIONAL ACCOUNTING STANDARDS</li>
                                <li class="topic-item">ADVANCED FINANCIAL RATIOS</li>
                                <li class="topic-item">CONSOLIDATED FINANCIAL STATEMENTS</li>
                            </ul>
                        </div>
                    </div>
                </div>
                
                <div class="upgrade-section">
                    <h3 class="upgrade-title">READY TO DOMINATE YOUR EXAMS?</h3>
                    <p>Our advanced DOMINATE mode is coming soon with even higher accuracy predictions</p>
                    <a href="#" class="upgrade-btn"><i class="fas fa-rocket"></i> JOIN WAITLIST FOR EARLY ACCESS</a>
                </div>
            </div>
        </div>
        
        <footer>
            <p>HACKSY &copy; 2023 | ETHICAL EXAM PREDICTION SYSTEM | ACCURACY: 89.7%</p>
        </footer>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const fileInput = document.getElementById('file-input');
            const fileInfo = document.getElementById('file-info');
            const fileName = document.getElementById('file-name');
            const progressContainer = document.querySelector('.progress-container');
            const progressBar = document.getElementById('progress-bar');
            const modes = document.querySelectorAll('.mode');
            const predictBtn = document.getElementById('predict-btn');
            const resultsSection = document.getElementById('results-section');
            const terminalText = document.querySelector('.terminal-text');
            
            let selectedMode = null;
            
            // File input change event
            fileInput.addEventListener('change', function() {
                if (this.files.length > 0) {
                    fileName.textContent = this.files[0].name.toUpperCase();
                    fileInfo.style.display = 'block';
                    progressContainer.style.display = 'block';
                    
                    // Simulate file upload and analysis
                    simulateFileAnalysis();
                    
                    // Update terminal text
                    terminalText.innerHTML = `
                        <div>> SYSTEM: FILE "${this.files[0].name.toUpperCase()}" RECEIVED</div>
                        <div>> ANALYZER: PROCESSING SYLLABUS CONTENT</div>
                        <div>> PREDICTION ENGINE: STANDBY</div>
                    `;
                    
                    // Auto-select the first mode
                    document.querySelector('.mode-1').click();
                } else {
                    fileName.textContent = 'NO FILE SELECTED';
                    fileInfo.style.display = 'none';
                    progressContainer.style.display = 'none';
                }
            });
            
            // Mode selection event
            modes.forEach(mode => {
                mode.addEventListener('click', function() {
                    if (this.classList.contains('locked')) {
                        // Show upgrade prompt for locked modes
                        terminalText.innerHTML += '<div>> ALERT: DOMINATE MODE COMING SOON. USING BE DECENT MODE.</div>';
                        document.querySelector('.upgrade-section').scrollIntoView({ behavior: 'smooth' });
                        
                        // Auto-select "Be Decent" mode instead
                        document.querySelector('.mode-2').click();
                        return;
                    }
                    
                    modes.forEach(m => m.classList.remove('selected'));
                    this.classList.add('selected');
                    selectedMode = this.getAttribute('data-mode');
                    
                    // Enable predict button if file is selected
                    if (fileInput.files.length > 0) {
                        predictBtn.disabled = false;
                    }
                    
                    // Update terminal text
                    terminalText.innerHTML += `<div>> MODE SELECTED: ${this.querySelector('.mode-title').textContent.toUpperCase()}</div>`;
                });
            });
            
            // Predict button event
            predictBtn.addEventListener('click', function() {
                if (!fileInput.files.length) {
                    terminalText.innerHTML += '<div>> ERROR: NO SYLLABUS FILE DETECTED</div>';
                    return;
                }
                
                if (!selectedMode) {
                    terminalText.innerHTML += '<div>> ERROR: NO PREDICTION MODE SELECTED</div>';
                    return;
                }
                
                // Simulate processing
                predictBtn.innerHTML = '<i class="fas极cog fa-spin"></i> ANALYZING...';
                predictBtn.disabled = true;
                
                // Update terminal text
                terminalText.innerHTML = `
                    <div>> SYSTEM: INITIATING PREDICTION SEQUENCE</div>
                    <div>> ANALYZER: DECRYPTING SYLLABUS CONTENT</div>
                    <极>> PREDICTION ENGINE: RUNNING AI MODELS</div>
                    <div>> STATUS: ESTIMATED TIME 5 SECONDS</div>
                `;
                
                // Simulate AI processing delay
                setTimeout(() => {
                    generatePrediction(selectedMode);
                    resultsSection.style.display = 'block';
                    predictBtn.innerHTML = '<i class="fas fa-brain"></i> INITIATE PREDICTION ANALYSIS';
                    predictBtn.disabled = false;
                    
                    // Update terminal text
                    terminalText.innerHTML += '<div>> PREDICTION: ANALYSIS COMPLETE - 89.7% ACCURACY</div>';
                    
                    // Scroll to results
                    resultsSection.scrollIntoView({ behavior: 'smooth' });
                }, 5000);
            });
            
            // Simulate file analysis progress
            function simulateFileAnalysis() {
                let width = 0;
                const interval = setInterval(() => {
                    if (width >= 100) {
                        clearInterval(interval);
                        predictBtn.disabled = false;
                        
                        // Update terminal text
                        terminalText.innerHTML += '<div>> ANALYZER: SYLLABUS PROCESSING COMPLETE</div>';
                    } else {
                        width += 5;
                        progressBar.style.width = width + '%';
                    }
                }, 100);
            }
            
            // Generate sample prediction based on mode
            function generatePrediction(mode) {
                // In a real app, this would be based on actual analysis
                // For the prototype, we'll use sample data
                
                const highTopics = document.getElementById('high-topics');
                const mediumTopics = document.getElementById('medium-topics');
                const lowTopics = document.getElementById('low-topics');
                
                // Clear previous results
                highTopics.innerHTML = '';
                mediumTopics.innerHTML = '';
                lowTopics.innerHTML = '';
                
                // Sample data for different modes
                const topics = {
                    'just-pass': {
                        high: [
                            'Basic accounting principles',
                            'Financial statements overview',
                            'Double-entry bookkeeping',
                            'Tax calculation fundamentals'
                        ],
                        medium: [
                            'Depreciation methods',
                            'Inventory valuation',
                            'Cash flow statements'
                        ],
                        low: [
                            'International accounting standards',
                            'Advanced financial ratios',
                            'Consolidated financial statements'
                        ]
                    },
                    'decent': {
                        high: [
                            'Financial statements analysis',
                            'Budgeting and forecasting',
                            'Cost-volume-profit analysis',
                            'Tax calculation methods',
                            'Depreciation methods'
                        ],
                        medium: [
                            'Working capital management',
                            'Standard costing',
                            'Cash flow management',
                            'Inventory valuation methods'
                        ],
                        low: [
                            'International accounting standards',
                            'Advanced financial ratios',
                            'Consolidated financial statements'
                        ]
                    }
                };
                
                // Populate topics based on selected mode
                topics[mode].high.forEach(topic => {
                    const li = document.createElement('li');
                    li.className = 'topic-item';
                    li.textContent = topic.toUpperCase();
                    highTopics.appendChild(li);
                });
                
                topics[mode].medium.forEach(topic => {
                    const li = document.createElement('li');
                    li.className = 'topic-item';
                    li.textContent = topic.toUpperCase();
                    mediumTopics.appendChild(li);
                });
                
                topics[mode].low.forEach(topic => {
                    const li = document.createElement('li');
                    li.className = 'topic-item';
                    li.textContent = topic.toUpperCase();
                    lowTopics.appendChild(li);
                });
            }
        });
    </script>
</body>
</html>
